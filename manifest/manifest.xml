<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<Package xmlns="http://soap.sforce.com/2006/04/metadata">
    <!-- Your existing tab -->
    <types>
        <members>dftlytime__Project_Hub</members>
        <name>CustomTab</name>
    </types>

    <!-- Agentforce Topic -->
    <types>
        <members>*</members>
        <name>GenAiPlugin</name>
    </types>

    <!-- Agentforce Action -->
    <types>
        <members>*</members>
        <name>GenAiFunction</name>
    </types>

    <!-- Prompt Template -->
    <types>
        <members>*</members>
        <name>GenAiPromptTemplate</name>
    </types>

    <!-- Keep your API version (works for these types on 60.0+; your 63.0 is fine) -->
    <version>63.0</version>
</Package>